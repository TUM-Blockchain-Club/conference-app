<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:label="Asmara"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">
        <!--
            configChanges: KoinApplication builds the DI container inside the
            composition, so an activity recreation would build a second
            container (new SupabaseClient, new SQLite driver) while Voyager's
            process-scoped ScreenModelStore keeps handing back models bound to
            the first one. Handling these configuration changes in-process
            avoids that until Koin is hoisted into the Application/entry points.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
